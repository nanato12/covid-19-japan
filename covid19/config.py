class Config:
    host = 'https://covid19-japan-web-api.now.sh/api/v1/'

    prefectures_url = host + 'prefectures'
    total_url = host + 'total'
    positives_url = host + 'positives'
    statistics_url = host + 'statistics'
